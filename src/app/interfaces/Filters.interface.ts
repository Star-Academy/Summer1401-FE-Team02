export interface Filters {
    status?: boolean;
    platforms?: number[];
    genres?: number[];
    gameModes?: number[];
    playerPerspectives?: number[];
    minimumRating?: number;
    maximumRating?: number;
}
